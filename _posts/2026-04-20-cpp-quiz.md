---
layout: post
title: "A silly little C++ quiz"
tags:
  - cpp
hn:
---

> _!!! This is a work in progress !!!_

> _Getting the answer formatting right took embarrassingly long,_
> _so I'll stop using this to procrastinate other things and finish the explanations another day._

For all the code snippets,
1. assume they are being run on an `x86_64` machine with `g++` and `-std=c++23` and
2. assume all necessary headers have been included

Hint: the code snippets might not all compile.

---

1. What is the value of `x` after the following code is executed?
    ```cpp
    int x = 0;
    x = x++;
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    `0`.
    
    First, `x++` evaluates to `0` (returns old value).  
    Then the increment side effect makes `x` become `1`, but assignment writes the old `0` back, so the final `x` is `0`.
    
    [godbolt link](https://godbolt.org/z/MdEKWscqq)
    </div>
    </details><br />
    

1. What is the value of `x` after the following code is executed?
    ```cpp
    int x[2] = {0,1};
    x[0], x[1] = x[1], x[0];
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. Given the following function `display`, what is the result of passing each of the following `vector<string>`s into `display`?
    ```cpp
    void display(const vector<string> &str_vec) {
        cout << str_vec.size() << endl;
        for (const string &str : str_vec) {
            cout << str << endl;
        }
    }
    ```

    ```cpp
    {% raw %}
    vector<string> foo{{"hello", ",", "world"}};
    vector<string> bar{{"hello", "world"}};
    vector<string> baz{{"hello", "hello"}};
    {% endraw %}
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What must the type/value of `foo` be for `foo["bar"] == 'a'` to be true?
    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What is the output of the following?
    ```cpp
    struct Bit {
        int b : 2 = 1;
    };

    int main() {
        Bit bit;
        cout << ++bit.b << endl;
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What does `const` modify in the following: the `int`, or the pointer to the `int`?
    ```cpp
    int const *ptr;
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What is the output of the following? Why?
    ```cpp
    void print(int &x) { cout << 1; }
    void print(int &&x) { cout << 2; }
    void print(const int &x) { cout << 3; }
    void print(const int &&x) { cout << 4; }

    int main() {
        int a = 3;
        print((const int)a);
        print((const int)1);
        print(4);
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. Given the empty struct `struct foo {}`, what is the size of `foo`?
    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. Given the struct below, what is the size of `foo`?
    ```cpp
    struct foo {
        uint8_t a;
        uint16_t b;
        uint8_t c;
        uint32_t d;
        uint8_t e;
    };
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What is the output of the following code?
    ```cpp
    #include <iostream>

    void f()
    {
        auto counter = []() {
            static int b = 1;
            return b++;
        };
        std::cout << counter();
    }

    int main()
    {
        f();
        f();
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What is the output of the following code?
    ```cpp
    #include <iostream>

    struct A {
        A() { std::cout << "A"; }
        ~A() { std::cout << "a"; }
    };

    int main() {
        return sizeof new A;
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What is the output of the following code?
    ```cpp
    #include <iostream>

    bool false() { std::cout << "false"; return false; }
    bool true() { std::cout << "true"; return true; }
    int bar() { std::cout << "bar"; return 0; }
    char baz() { std::cout << "baz"; return 0; }

    int main() {
        return false() && true() ? bar() : baz();
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. In the following code:
    1. How many times is `foo`’s copy constructor called?
    1. How many times is `foo`’s parameterized constructor called?
    
    ```cpp
    struct foo {
        int x;
        // Parameterized constructor
        foo(int _x) : x(_x) {}
        // Copy constructor
        foo(foo &other) : x(other.x) {}
    };

    struct bar {
        foo x;
        foo y;
        bar(foo _x, foo _y) : x(_x), y(_y) {}
    };

    int main() {
        foo x{1};
        foo y{2};
        bar{x,y};
        return 0;
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. Identify each stack and heap allocation in the following:
    ```cpp
    #include <memory>

    int main() {
        int x = 42;
        int *y = new int(8);
        auto z = std::make_shared<int>(-50);
        int result = x + *y + *z;
        delete y;
        return result;
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What does the following print out?
    ```cpp
    int x = 010;
    std::cout << x << std::endl;
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. When does each variable get allocated and deallocated?
    ```cpp
    int a = 0;

    void foo() {
        int b = 1;
        static int c = 2;
        {
            int d = 3;
            static int e = 4;
            const int f = 5;
        }
    }

    int main() {
        foo();
        return 0;
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


1. What does the following code evaluate to? Why might we choose to write code this way?
    ```cpp
    #include <iostream>

    template<typename T>
    auto f(T t) -> decltype(t.foo(), int{}) { return 1; }

    template<typename T>
    auto f(T t) -> decltype(t + 1, int{}) { return 2; }

    int f(...) { return 3; }

    struct A { void foo(); };
    struct B {};

    int main() {
        std::cout << f(A{}) << f(B{}) << f("hi");
    }
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    <!-- TODO -->
    </div>
    </details><br />


<!--1. What does a template question look like?
    ```cpp
    code snippet
    ```

    <details><summary class="details-summary">Answer/Explanation</summary>
    <div class="details-body" markdown="1">
    It looks like this!
    
    Here's some rationale. Maybe I even add a
    ```
    code block
    ```
    
    [godbolt link](https://godbolt.org/z/MdEKWscqq)
    </div>
    </details><br />-->
